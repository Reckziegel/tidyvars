% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scales-tidyvars.R
\name{scale_color_tidyvars}
\alias{scale_color_tidyvars}
\alias{scale_fill_tidyvars}
\title{tidyvars discrete colour scales}
\usage{
scale_color_tidyvars(..., na.value = "grey70")

scale_fill_tidyvars(..., na.value = "grey70")
}
\arguments{
\item{...}{Additional arguments passed to [ggplot2::discrete_scale()].}

\item{na.value}{Colour used for missing values.}
}
\value{
A discrete ggplot2 scale.
}
\description{
Discrete colour and fill scales designed for econometric and financial
graphics produced with tidyvars.
}
\details{
`scale_color_tidyvars()` controls mapped `colour` aesthetics, while
`scale_fill_tidyvars()` controls mapped `fill` aesthetics.

These scales are independent from [theme_tidyvars()]. Applying the theme
does not modify data colours.
}
\examples{
library(ggplot2)

ggplot(
  mtcars,
  aes(wt, mpg, colour = factor(cyl))
) +
  geom_point(size = 3) +
  scale_color_tidyvars()

ggplot(
  mtcars,
  aes(factor(cyl), fill = factor(gear))
) +
  geom_bar() +
  scale_fill_tidyvars()

}
